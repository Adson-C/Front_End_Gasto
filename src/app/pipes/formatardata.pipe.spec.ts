import { FormatardataPipe } from './formatardata.pipe';

describe('FormatardataPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatardataPipe();
    expect(pipe).toBeTruthy();
  });
});
